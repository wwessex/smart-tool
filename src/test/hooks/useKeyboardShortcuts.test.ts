import { describe, it } from "vitest";

describe("useKeyboardShortcuts", () => {
  it.todo("registers shortcuts and tears them down on unmount");
  it.todo("detects key combinations with modifier keys");
  it.todo("invokes handlers only when shortcuts match");
});
