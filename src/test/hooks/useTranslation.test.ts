import { describe, it } from "vitest";

describe("useTranslation", () => {
  it.todo("manages translation state and updates on language change");
  it.todo("selects the requested language when available");
  it.todo("falls back to local translations when missing remote strings");
});
