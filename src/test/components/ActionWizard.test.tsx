import { describe, it } from "vitest";

describe("ActionWizard", () => {
  it.todo("navigates between steps and preserves input state");
  it.todo("validates required fields before proceeding");
  it.todo("summarizes final action before submission");
});
