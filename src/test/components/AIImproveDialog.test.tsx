import { describe, it } from "vitest";

describe("AIImproveDialog", () => {
  it.todo("opens and closes the dialog");
  it.todo("shows loading state while improvements are generated");
  it.todo("handles error states from AI responses");
});
