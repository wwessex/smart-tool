name: Apply ZIP package to repo

on:
  workflow_dispatch:
    inputs:
      zip_path:
        description: "Path to ZIP file in the repo (e.g. imports/smart-tool-admin-playbook.zip)"
        required: true
        default: "imports/smart-tool-admin-playbook.zip"
      target_branch:
        description: "Branch to apply to"
        required: true
        default: "main"
      clean_repo:
        description: "Delete existing files before applying ZIP (keeps .git + imports)"
        required: true
        default: "true"

permissions:
  contents: write

jobs:
  apply_zip:
    runs-on: ubuntu-latest
    env:
      PAT: ${{ secrets.REPO_PAT }}

    steps:
      - name: Checkout target branch into ./repo
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.target_branch }}
          fetch-depth: 0
          path: repo
          persist-credentials: false

      - name: Sanity check checkout
        shell: bash
        run: |
          set -euo pipefail
          test -d repo/.git
          echo "Checked out: $(git -C repo rev-parse --short HEAD)"

      - name: Resolve ZIP path
        id: zippath
        shell: bash
        run: |
          set -euo pipefail
          Z="${{ inputs.zip_path }}"
          if [ ! -f "repo/$Z" ] && [ -f "repo/imports/$Z" ]; then
            Z="imports/$Z"
          fi
          if [ ! -f "repo/$Z" ]; then
            echo "ZIP not found: repo/$Z"
            echo "repo root:"
            ls -la repo
            echo "repo/imports:"
            ls -la repo/imports || true
            exit 1
          fi
          echo "zip=$Z" >> "$GITHUB_OUTPUT"
          echo "Using ZIP: $Z"
          sha256sum "repo/$Z" || true

      - name: Extract ZIP to temp
        shell: bash
        run: |
          set -euo pipefail
          rm -rf /tmp/applyzip
          mkdir -p /tmp/applyzip
          unzip -q "$GITHUB_WORKSPACE/repo/${{ steps.zippath.outputs.zip }}" -d /tmp/applyzip
          ls -la /tmp/applyzip

      - name: Detect payload root
        id: detect
        shell: bash
        run: |
          set -euo pipefail
          ROOT="/tmp/applyzip"
          DCOUNT="$(find "$ROOT" -mindepth 1 -maxdepth 1 -type d | wc -l | tr -d ' ')"
          FCOUNT="$(find "$ROOT" -mindepth 1 -maxdepth 1 -type f | wc -l | tr -d ' ')"
          if [ "$DCOUNT" -eq 1 ] && [ "$FCOUNT" -eq 0 ]; then
            ROOT="$(find "$ROOT" -mindepth 1 -maxdepth 1 -type d | head -n 1)"
          fi
          echo "root=$ROOT" >> "$GITHUB_OUTPUT"
          echo "Payload root: $ROOT"
          ls -la "$ROOT"

      - name: Validate payload
        shell: bash
        run: |
          set -euo pipefail
          ROOT='${{ steps.detect.outputs.root }}'
          test -f "$ROOT/package.json"
          test -f "$ROOT/index.html"
          test -d "$ROOT/src"
          test -d "$ROOT/public"
          echo "Payload validation OK."

      - name: Optional clean repo (keep .git + imports)
        if: ${{ inputs.clean_repo == 'true' }}
        shell: bash
        run: |
          set -euo pipefail
          test -d repo/.git
          find repo -mindepth 1 -maxdepth 1             ! -name ".git"             ! -name "imports"             -exec rm -rf {} +

      - name: Apply ZIP into repo (overwrite, keep .git + imports)
        shell: bash
        run: |
          set -euo pipefail
          ROOT='${{ steps.detect.outputs.root }}'
          test -d repo/.git
          rsync -a --delete             --exclude '.git/'             --exclude 'imports/'             "$ROOT/" "repo/"
          test -d repo/.git

      - name: Assert critical fixes are present (fail fast)
        shell: bash
        run: |
          set -euo pipefail

          # If this fails, you're NOT applying the ZIP you think you are.
          if grep -RIn -- '\\[0-9][0-9]*' repo/src/index.css; then
            echo "::error::Found illegal CSS escapes like \\1232 in src/index.css"
            exit 1
          fi

          if grep -n -- "href=\"\./loader\.css\"" repo/index.html; then
            echo "::error::index.html still references ./loader.css (should be /loader.css)"
            exit 1
          fi

          test -f repo/public/loader.css || (echo "::error::public/loader.css missing" && exit 1)

          if grep -RIn -- 'border-white/' repo/src/index.css; then
  echo "::error::Found border-white/ opacity utility in src/index.css @apply; remove to avoid PostCSS parse issues"
  exit 1
fi

echo "Assertions OK."

      - name: Commit and push via PAT
        shell: bash
        working-directory: repo
        run: |
          set -euo pipefail
          test -n "${PAT:-}" || (echo "Missing REPO_PAT secret" && exit 1)

          OWNER="${GITHUB_REPOSITORY_OWNER}"
          REPO="${GITHUB_REPOSITORY#*/}"

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://${PAT}@github.com/${OWNER}/${REPO}.git"

          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git commit -m "Apply ZIP package"
          git push origin "${{ inputs.target_branch }}"
